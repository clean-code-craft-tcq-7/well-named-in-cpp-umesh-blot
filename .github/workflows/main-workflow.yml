name: Build and Run

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: g++ -std=c++17 -Wall -Wextra -o colorcoder -I prj/src/*.cpp test/src/*.cpp 

    - name: run
      run: ./colorcoder
